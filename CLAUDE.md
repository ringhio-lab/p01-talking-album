# このリポジトリで作業するときに知るべきこと

2歳の息子と一緒に使う PWA。**写真をタップすると父親の声で名前を言う。**

- 公開URL: https://shoki-sato.github.io/project_01-talking-album/
- 構成: `index.html` / `app.js` / `sw.js` / `manifest.webmanifest` / `icon-*.png` の8ファイル・21KB
- **フレームワークなし・ビルドなし・CIなし。**この規模に要らない。増やさない

## 次にやること（未着手）

**実機での動作確認。配信までしか検証していない。**iPhone Safari で URL を開き、ホーム画面に追加して、以下を1つずつ試す。

1. 「写真をえらぶ」で写真が取り込めるか
2. **「ろくおん」で録音できるか** ← 最も壊れやすい
3. 写真タップで音が鳴るか
4. 機内モードで動くか（Service Worker）

**直したら push するだけで iPhone に即反映される**（ストア審査も再インストールも不要）。これが PWA を選んだ理由。

## 変えてはいけない設計

### 写真と音声をリポジトリに入れない

**端末の IndexedDB のみに置く。**GitHub Free では Private リポジトリの Pages が使えないため（実測 `has_pages: false`）、写真を同梱すると配信できない。**アプリ本体だけを Public に置き、個人データが外に出る経路を構造的に無くしている。**

`.gitignore` が `*.jpg` `*.m4a` 等を全ブロック（`icon-*.png` のみ例外）。負テスト済み。**ここを緩めない。**

### 2歳児向けのUI判断

- **親モードの入口は画面上端の1秒長押し。**見えるボタンを置くと子供が押すため、意図的に隠している
- **大きい写真はどこを触っても閉じる。**2歳児に閉じるボタンは見つけられない
- テキストに頼らない（読めない）

## 実装上の注意（踏むと分かりにくい）

- **iOS Safari の `MediaRecorder` は `webm` を出さない。**`audio/mp4` になる。`pickMime()` で吸収しているが実機未確認
- **iOS は音声再生にユーザー操作を要求する。**タップハンドラ内で `play()` を呼んでいるので成立している。ここを非同期の外へ出すと鳴らなくなる
- **IndexedDB は iOS に消される可能性がある。**写真の原本は端末の写真アプリに残す前提（README記載）
- Service Worker は HTTPS 必須（GitHub Pages が満たす）

## 環境

- **Repo 所有者は `SHOKI-SATO`。`gh` の既定アクティブは `SATOSHOKI`。**操作前に `gh auth switch --user SHOKI-SATO` が要る（両アカウントとも認証済み・同一人物）
- **`gh` に `delete_repo` スコープが無い。**Repo削除はブラウザで行うか `gh auth refresh -h github.com -s delete_repo`（ブラウザ承認が必要）
- 録音は親の手作業。**10枚程度に留める**（2歳児は10枚を100回見る。多いと続かない）

## 経緯

事業として成立する副業を探すのをやめ、**自分が作りたいものを作る**モードに切り替えた1本目。

選定の経緯・落とした案とその理由・次の候補（v2はおでかけ写真→絵本）は
`Documents/01_Obsidian/70_PRIVATE/個人サービス/01_ドキュメント_アイデア/個人制作モード_案リスト_2026-07-15.md` にある。
